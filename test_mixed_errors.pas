program Test;
var
  x, y: integer;
  z: real;
  undeclared_var: string; { будет объявлена, но не использована - предупреждение }
begin
  x := 10;
  y := x + 5;
  z := 3.14;
  
  { Семантические ошибки }
  missing_var := 100;  { необъявленная переменная }
  x := z;              { несовместимые типы }
  y := "hello";        { несовместимые типы }
  
  { Синтаксическая ошибка }
  x := 1 + ;           { неполное выражение }
  
  writeln(x, y, z)
end.
